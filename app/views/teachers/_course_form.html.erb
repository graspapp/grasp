<%= form_for Course.new, :url => courses_path do |f| %>
  <fieldset>
    <div class="form-group">
      <%= f.text_field :name,
        class: 'form-control',
        placeholder: "Course name" %>
    </div>
    <%= f.submit "Create Course", class: "btn btn-default"%>
  </fieldset>
<% end %>
