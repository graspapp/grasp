<% if signed_in? %>
<% provide(:title, 'Teacher Profile') %>

<div class="page-header">
  <h1><%= @teacher.first_name %> <%= @teacher.last_name %></h1>
</div>

<div class="row-fluid">
  <div class="span12">   
    <%= render 'dashboard' %>
    <%= render 'course_modal'%>
  </div>
</div>
<% end %>